# Comic Book Flipbook

A Streamlit app that displays a digital comic book with a 3D page-turn animation.

Run locally:

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

## Controls

- Click the left/right halves of a page (or use the arrow buttons / arrow keys) to turn pages
- Touch swipe on mobile
